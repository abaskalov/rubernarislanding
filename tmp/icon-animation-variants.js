// Figma Component with Animation Variants
// Node: 54-2720 - Cloud Icon Animation

// Three variants detected - likely keyframes for animation:
// 1. Default: 526.289px × 500px (normal state)
// 2. Variant4: 506.904px × 435.451px (slightly smaller)
// 3. Variant5: 667.395px × 661.464px with 180deg rotation (flipped and larger)

// These variants suggest a morphing/transforming animation for the cloud icon
// The icon changes size and rotates between states

// Animation sequence likely:
// Default → Variant4 (shrink) → Variant5 (grow + rotate)
// Or could be a cycle through all three states
